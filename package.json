{
  "name": "string-reverser",
  "version": "1.0.0",
  "description": "A string reverser web app built with TDD using Jest",
  "scripts": {
    "test": "jest --verbose",
    "test:watch": "jest --watch --verbose"
  },
  "jest": {
    "testEnvironment": "jsdom"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}